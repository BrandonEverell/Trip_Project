<%- include('./partials/header') %>
  <div id="welcomebg" class="d-flex justify-content-center align-items-center">
<%# justify-content-center align-items-center%>
  <%# <div id="login" class="card text-center" style="width: 20rem;">
  <div class="card-block">
    <h4 class="card-title">Log In</h4>
    <p class="card-text">Please log in with your facebook or google account</p>
    <a href="#" class="btn btn-primary">Go somewhere</a>
  </div>
</div>
</div>%>

<% if(!currentUser) { %>
  <div id="login" class=" col-md-3 text-center">
  <div class="card bg-light">
  <h2 class="card-header bg-success text-white ">Log In</h2>
  <div class="card-body">
  <form class="d-flex flex-column" action="/session" method="POST">
     <div class="form-group">
       <input
         class="form-control"
         name="email"
         placeholder="Email"
         type="email"
       />
     </div>

     <div class="form-group">
       <input
         class="form-control"
         name="password"
         placeholder="Password"
         type="password"
       />
     </div>

     <input
       class="btn btn-raised btn-primary d-flex align-self-center text-center"
       type="submit"
       value="Log In"
     />
   </form>
   <%# <a href="/auth/facebook" class="btn btn-primary">Facebook Login</a>
   <a href="/auth/google" class="btn btn-primary">Google Login</a>%>
  <a href="/users/new" class="btn btn-primary"> "Don't have an account? Sign up"</a>

</div>
</div>
</div>
<% } else { %>
<h1 class="welcomePage"> Welcome </h1>
  <% } %>
</div>

<%- include('./partials/footer') %>
